"""L4 — the precomputed artifact the static site reads.

`spec.py` is the seam: the format is declared there once, and the Python codec, the
TypeScript types and the reference TypeScript reader are all generated from it.
"""
